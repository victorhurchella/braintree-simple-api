# Braintree Simple API

This repo is just a simple braintree integration.

### How to use

- Clone or download .zip repository;
- `npm install` or `yarn` to install libs;
- Config keys/ ids in `.env`;
- `npm run dev` or `yarn dev` to start api.
